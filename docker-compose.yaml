postgres:
  ports:
    - '5436:5432'
  restart: always
  image: postgres:9.6.5-alpine
  enviroments:
    - DB_USERNAME: 'admin'
    - DB_PASSWORD: 'admin'
  volumes:
    - ./db/postgres-initdb.sh:/docker-entrypoint-initdb.d/init.sh
