export  const forumService = (_req: Request, res: Response) => {
    /*
    return userThemeRep
      .getByUserId(user.id)
      .then(data => res.status(200).json(data))
      .catch(err =>
        res.status(500).json({ error: { type: "db error", data: err } }),
      );*/
  };
